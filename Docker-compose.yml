version: '3'

services:

  frontend:
    image: soundgiph-frontend:latest
    ports:
      - 3000:3000
    depends_on:
      - backend

  backend:
    image: soundgiph-backend:latest
    ports: 
      - 8080:8080
    restart: always
  
